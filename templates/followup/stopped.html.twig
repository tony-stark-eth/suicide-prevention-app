{% extends 'base.html.twig' %}

{% block title %}{{ 'followup.stopped_title'|trans }} — {{ 'app.title'|trans }}{% endblock %}

{% block body %}
<div class="max-w-md mx-auto px-4 py-24 text-center">
    <h1 class="text-2xl font-light text-stone-100 mb-4">{{ 'followup.stopped_title'|trans }}</h1>
    <p class="text-stone-400 text-sm mb-8">{{ 'followup.stopped_body'|trans }}</p>
    <a href="{{ path('home') }}" class="text-blue-400 hover:text-blue-300 text-sm">← Zurück</a>
</div>
{% endblock %}
